---
## 1st Run
- hosts: localhost
  become: no

  vars:
    test: yes
    ec2_instances: 1

  roles:
    - ansible-role-aws-cloud

## 2nd Run for idempotent
- hosts: localhost
  become: no

  vars:
    test: yes
    ec2_instances: 1

  roles:
    - ansible-role-aws-cloud

## 3rd Run for cleanup
- hosts: localhost
  become: no

  vars:
    test: yes
    ec2_instances: 0

  roles:
    - ansible-role-aws-cloud
